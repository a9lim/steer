Screen {
    layout: vertical;
    background: ansi_default;
    color: ansi_white;
}

/* Force all widgets to inherit terminal colors */
Widget {
    background: ansi_default;
    color: ansi_white;
    scrollbar-color: ansi_bright_black;
    scrollbar-color-hover: ansi_white;
    scrollbar-color-active: ansi_white;
    scrollbar-background: ansi_default;
}

#main-area {
    height: 1fr;
    layout: horizontal;
}

/* Three-column layout: left (1fr) | center (2fr) | right (1fr) */
#left-panel {
    width: 1fr;
    height: 100%;
    border-right: solid ansi_white;
    padding: 0 1;
    layout: vertical;
}

#chat-panel {
    width: 2fr;
    height: 100%;
    border: solid ansi_black;
    layout: vertical;
}

#trait-panel {
    width: 1fr;
    height: 100%;
    border-left: solid ansi_white;
    padding: 0 1;
    layout: vertical;
}

/* Panel focus highlighting */
#left-panel.focused {
    border-right: double ansi_yellow;
}

#chat-panel.focused {
    border: solid ansi_yellow;
}

#trait-panel.focused {
    border-left: double ansi_yellow;
}

/* Left panel sections */
.section-header {
    color: ansi_yellow;
    text-style: bold;
    height: 1;
}

.section-body {
    height: auto;
    padding: 0;
}

#model-section {
    height: auto;
}

#vectors-section {
    height: 1fr;
    min-height: 5;
}

#vector-scroll {
    height: 1fr;
    overflow-y: auto;
}

#gen-section {
    height: auto;
}

#keys-section {
    height: auto;
}

.vector-row {
    height: auto;
}

.vector-row-selected {
    height: auto;
    background: ansi_default;
}

/* Chat panel internals */
#chat-log {
    height: 1fr;
    overflow-y: auto;
    padding: 0 1;
}

#status-bar {
    height: 1;
    background: ansi_default;
    color: ansi_bright_black;
    padding: 0 1;
}

#chat-input {
    height: 3;
    dock: bottom;
    border: solid ansi_black;
    background: ansi_default;
    color: ansi_white;
}

/* Override Textual's default focus border */
#chat-input:focus {
    border: solid ansi_black;
}

.user-message {
    margin: 1 0 0 0;
}

.assistant-message {
    margin: 0 0 1 0;
}

/* Trait panel internals */
#trait-header {
    height: 1;
}

#trait-scroll {
    height: 1fr;
    overflow-y: auto;
}

.trait-category {
    text-style: bold;
    color: ansi_yellow;
    height: 1;
}

.trait-row {
    height: 1;
}

.trait-row-selected {
    height: auto;
    background: ansi_default;
}

#trait-hints {
    height: 1;
    color: ansi_bright_black;
}
